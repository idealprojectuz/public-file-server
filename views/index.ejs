<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <ul>
        ${files.forEach(
        (e) =>
        `<li> <a href="${req.headers.host}/${encodeURIComponent(
              e
            )}" download>${e}</a> </li>`
        )}

    </ul>
</body>

</html>